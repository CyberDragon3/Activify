{
  "name": "extension",
  "version": "1.2.3",
  "description": "A Chrome side-panel planner that scans school platforms on demand and helps students build schedules with Groq-powered AI.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:storage": "esbuild shared/storage.js --bundle --outfile=shared/storage.bundle.js --format=esm --platform=browser",
    "build:analytics": "esbuild shared/analytics.js --bundle --outfile=shared/analytics.bundle.js --format=esm --platform=browser",
    "build": "npm run build:storage && npm run build:analytics"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.27.4"
  },
  "dependencies": {
    "posthog-js": "^1.376.0"
  }
}
