{
  "name": "extension",
  "version": "1.2.0",
  "description": "A Chrome side-panel planner that scans school platforms on demand and helps students build schedules with Groq-powered AI.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild shared/storage.js --bundle --outfile=shared/storage.bundle.js --format=esm --platform=browser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "./supabase-lib.js": "^2.99.3"
  },
  "devDependencies": {
    "esbuild": "^0.27.4"
  }
}
